# 🤖 Autonomous QA Agent for Test Case and Script Generation

## 🚀 Features
- **Microservices Setup:** Decoupled Architecture using FastAPI (Backend) and Streamlit (Frontend).
- **Knowledge Base Ingestion:** Uploads PDFs, Markdown, and HTML to create a local vector database (ChromaDB).
- **Test Case Generation:** AI Agent (Gemini 2.5 Flash) creates positive/negative test scenarios based on requirements.
- **Auto-Scripting:** Generates ready-to-run Python Selenium scripts mapped to the specific HTML structure.
- **Self-Healing Scripts:** Generated scripts automatically handle driver installation (`webdriver-manager`) and path resolution.

## 🛠️ Tech Stack
- **Frontend:** Streamlit
- **Backend:** FastAPI, Uvicorn
- **AI Model:** Google Gemini 2.5 Flash
- **Orchestration:** LangChain
- **Database:** ChromaDB (Vector Store)
- **Automation:** Selenium WebDriver (Python)

## 📂 Project Structure
```bash
Intelligent QA Tester/
├── api.py                 # Backend Server (FastAPI) - Runs on Port 8000
├── app.py                 # Frontend UI (Streamlit) - Runs on Port 8501
├── vector_db.py           # Logic: Document Processing & ChromaDB
├── llm_helper.py          # Logic: RAG Pipeline & Gemini Integration
├── test_checkout.py       # Output: Generated Selenium Script
├── requirements.txt       # Dependencies
├── .env                   # API Keys (Not uploaded to GitHub)
└── project_assets/        # Folder containing checkout.html & docs

## ⚙️ Installation & Setup

Follow these steps to set up the project locally.

### 1. Clone the Repository
Download the project code to your local machine.

---------------------------
git clone <repo-link>
cd "Intelligent QA Tester"
---------------------------

### 2. Set Up Virtual Environment
It is recommended to use a virtual environment to manage dependencies.

**For Windows:**
---------------------------
python -m venv venv
.\venv\Scripts\activate
---------------------------

**For Mac/Linux:**
---------------------------
python3 -m venv venv
source venv/bin/activate
---------------------------

### 3. Install Dependencies
---------------------------
pip install -r requirements.txt
---------------------------

### 4. Configure API Keys
---------------------------
Get your free API Key from Google AI Studio.

Create a file named **.env** in the root folder.

Paste your key inside it:
**GEMINI_API_KEY=api_key**
---------------------------

### 5. Open two terminals
---------------------------
1:- uvicorn api:app --reload 
2:- streamlit run app.py
---------------------------

###6. End
---------------------------
Copy the Code: Copy the Python code block generated by the agent inside the UI.
Create a File: Create a new file named test_checkout.py in the main project folder.
Paste & Save: Paste the code into the file.

**Run the Test:**

python test_checkout.py
---------------------------